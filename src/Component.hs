module Component (module Export, module Component) where

import Ef.Base as Export hiding (As,Index,watch,transform,construct,uncons,distribute,embed,observe,End,Nat(..),maps)
import Atomic.Component as Export
import Atomic as Export hiding (path,maps)
import Prelude as Export hiding (all,exponent,reverse,tan,lookup)

